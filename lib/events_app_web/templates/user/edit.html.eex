<h1>Edit User</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.user_path(@conn, :update, @user)) %>

<span><%= link "Delete", to: Routes.user_path(@conn, :delete, @user), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-outline-danger" %></span>
<span><%= link "View All Profiles", to: Routes.user_path(@conn, :index), class: "btn btn-outline-primary" %></span>
