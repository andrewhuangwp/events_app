<h1>Create New Event</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.event_path(@conn, :create)) %>

<a><%= link "Back to Events List", to: Routes.event_path(@conn, :index), class: "btn btn-outline-primary" %></a>
